import { sleep, getRandomNumber } from './promises'

/*
  - Create function that first generate a random number and logs it to the console.
  - Then wait N seconds, where N is the preivously generated random number.
  - Then prints "Done" to the console.
  - Use the sleep() and getRandomNumber() functions for it.
  - You can choose between the then or the async - await syntax.
*/

const task = () => {

}

task()